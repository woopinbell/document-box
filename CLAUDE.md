# CLAUDE.md

`~/Desktop/seungwoo` 워크스페이스 **라우터**. auto-load되므로 **얇게 유지 — 룰 본문은 여기 두지 않는다.**
의도를 보고 해당 정본을 읽어라.

## 지금 국면 (2026-06~)

코퍼스(40 레포)는 **다 지어졌고** clean·원격 동기화 상태다. **더 짓지 않는다(포화).** 지금 할 일은 둘:

1. **학습/체화** [메인 트랙] — 지은 것을 실력으로 전환. 코드 재현이 아니라 판단력(재구성)+AI 결함
   잡기(리뷰). → `document-box/STUDY.md`
2. **갭 메우기(출하·취업)** [병렬 트랙] — 라이브 제품·OSS·기술 글·취업 파이프라인. → `launch-box/`

## 의도별 정본 (읽고 따르라)

| 하려는 일 | 읽을 문서 |
|---|---|
| 레포 **학습/복기** | `document-box/STUDY.md`(실전 압축) · `LEARNING.md`(정본) · `INTERVIEW.md`(L3 백지 세트) |
| **출하·취업**(갭 메우기) | `launch-box/README.md` → `SHIP.md` · `HIRE.md` · `ledger.md` |
| 워크스페이스 **전체 파악** | `document-box/WORKSPACE.md` |
| **코퍼스 작업**(빌드/품질/검수/커밋 규율) | `document-box/corpus-router.md`(작업 종류별 정본 색인 — 구 루트 라우터) |

## 공통 불변

- **추측·환각 금지** — 근거(`git show`/실소스)를 못 찾으면 멈추고 보고.
- 커밋: 영어 명령형 제목 + 한국어 `[근거]/[변경]/[검증]`, **AI 트레일러 없음**, 근무시간 09:00–21:59 KST
  (상세 `document-box/commit-policy.md`).
- **새 코퍼스 학습 콘텐츠는 만들지 않는다** — 출하·취업은 *기존 자산을 활용*한다.
- 허브: `document-box`(거버넌스·git) · `plan-box`(기획·비-git) · `launch-box`(출하·취업·**private git**).

> 라우터는 의도적으로 얇다. 세부 룰·기준·절차는 위 정본에 있다 — 여기에 새 룰을 적지 말 것.
