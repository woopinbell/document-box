# CLAUDE.md

`~/Desktop/seungwoo` 워크스페이스 **라우터**. auto-load되므로 **얇게 유지 — 룰 본문은 여기 두지 않는다.**
의도를 보고 해당 정본을 읽어라.

## 지금 국면 (2026-06~)

코퍼스(현행 대상은 `document-box/AUDIT.md`의 inventory)는 **다 지어졌고** clean·원격 동기화 상태다.
독립 신규 학습 주제를 더 짓지 않는다(포화). 지금 할 일은 둘:

1. **학습/체화** [메인 트랙] — 지은 것을 실력으로 전환. 코드 재현이 아니라 판단력(재구성)+AI 결함
   잡기(리뷰). → `document-box/STUDY.md`
2. **갭 메우기(출하·취업)** [병렬 트랙] — 라이브 제품·OSS·기술 글·취업 파이프라인. → `launch-box/`

## 의도별 정본 (읽고 따르라)

| 하려는 일 | 읽을 문서 |
|---|---|
| 레포 **학습/복기** | `document-box/STUDY.md`(**학습 정본** — 전 분야·순서·완주 단일 문서) · `INTERVIEW.md`(L3 백지 부록). 구 `SEQUENCE`·`LEARNING`·`mobile-track`은 STUDY에 흡수된 **동결 레거시** |
| **출하·취업**(갭 메우기) | `launch-box/README.md` → `SHIP.md` · `HIRE.md` · `ledger.md` |
| 워크스페이스 **전체 파악** | `document-box/WORKSPACE.md` |
| **코퍼스 작업**(빌드/품질/검수/커밋 규율) | `document-box/corpus-router.md`(작업 종류별 정본 색인 — 구 루트 라우터) |

## 작업 규칙 라우팅

- 코퍼스의 build·from-zero dual-form·감사·품질·commit 규칙은
  `document-box/corpus-router.md`에서 작업 종류별 정본으로 이동한다.
- 이력 재작성과 from-zero 집필이 결합되면 그 라우터의 **이력 재작성 + from-zero dual-form** 복합
  경로를 그대로 따른다. 세부 규칙을 이 auto-load 파일에 복제하지 않는다.
- 현행 감사 대상과 허브 제외 목록은 `document-box/AUDIT.md` §1을 따른다.

> 라우터는 의도적으로 얇다. 세부 룰·기준·절차는 위 정본에 있다 — 여기에 새 룰을 적지 말 것.
